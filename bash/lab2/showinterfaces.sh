#!/bin/bash
#
#Create an array using the output of ifconfig which has the names of the network interfaces in it. Use those names to extract the ip addresses of the interfaces, also using ifconfig output. Also parse the output of route -n to display the ip address of the default gateway. Name this script showinterfaces.sh.



